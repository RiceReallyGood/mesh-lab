样本数: 1000 条 trace

── 端到端分解（avg，可相加）
  kitex-client 自身                  16.3µs     6.2%
  UDS往返 kitex-client↔envoy-out     20.1µs     7.6%
  envoy-out 自身                     26.4µs    10.0%
  跨机往返 envoy-out↔envoy-in       110.6µs    41.8%
  envoy-in 自身                      44.5µs    16.8%
  UDS往返 envoy-in↔kitex-server      12.4µs     4.7%
  kitex-server 自身                  34.1µs    12.9%
  ───────────────                  ────────   ─────
  合计                              264.5µs   100.0%
  端到端（client 总时长）           264.5µs

── 这两个量到底是什么

  设「N 总」= 节点 N 观测到的最早点到最晚点，
    「N 等」= 节点 N 花在等下一跳身上的时间，取值为：
        Envoy 各跳     up_write_done → up_epoll_wake   （请求写出 → 响应把 epoll 唤醒）
        kitex-client   mesh_socket_read_start → mesh_first_byte

  **「N 自身」= N 总 − N 等**
      N 真正占用 CPU 的时间：解析、路由、编解码、业务逻辑。
      **不含**它坐等下一跳的那段。链路末端（server）不等任何人，自身 = 总。

  **「A↔B 往返」= A 等 − B 总**
      A 在等 B 的那段里，扣掉 B 自己干活的时间，剩下的就是数据在两者之间
      走了一个来回（去程 + 回程）。两项各自在本机用单调钟测得，
      相减时时钟偏斜完全抵消 —— 这是跨机也成立的原因（§8.2.3）。

  两式交替套用即可从 client 一路推到 server，各段相加恰好等于端到端
  （望远镜求和：中间每个「N 总」都被加一次减一次）。

  三条限制：
  1. **只有 avg 能这么相加，分位数不可加** —— 所以本表只给 avg（§9.3 ①）。
  2. **「往返」不可拆分单向**：跨机是物理限制（需 PTP + 硬件时间戳网卡）；
     同机 UDS 同理 —— 差值法给出的本来就是一个来回。
  3. **「往返」不是纯线路时间**：B 起算点之前的排队（listener/worker 队列、
     goroutine 调度）落在 B 的测量区间之外，全被这个差值吸收。
     实测 Envoy worker 从 384 压到 2 时，同机 UDS「往返」从 21µs 涨到 128µs——
     多出来的一百微秒是排队不是传输。低负载无排队时才近似真实传输时间。

── 各节点观测区间（**嵌套，不可相加**，差值法的原始输入）
区间                          avg        p50        p90        p99        max
链路开销(往返)                230.4µs    196.7µs    338.3µs    470.2µs    5.663ms
envoy-in                 91.0µs     76.1µs    105.3µs    244.9µs    1.155ms
envoy-out               228.1µs    199.3µs    343.3µs    493.5µs    1.608ms
kitex-client            264.5µs    228.1µs    373.2µs    538.8µs    5.943ms
kitex-server             34.1µs     28.1µs     45.3µs    104.4µs    1.017ms

注：涉及跨机，「链路开销」为往返总和，不可拆分单向（§8.2.4）
