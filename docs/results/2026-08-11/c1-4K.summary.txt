样本数: 2930 条 trace

── 端到端分解
                                        avg     占比        p50        p90        p99
  kitex-client 自身                  22.8µs   6.1%     20.7µs     27.9µs     51.9µs
  UDS往返 kitex-client↔envoy-out      3.3µs   0.9%      3.4µs      4.4µs      9.6µs
  envoy-out 自身                     46.8µs  12.5%     39.6µs     46.4µs    169.8µs
  跨机往返 envoy-out↔envoy-in       182.0µs  48.7%    177.9µs    188.0µs    217.6µs
  envoy-in 自身                      74.4µs  19.9%     68.5µs     73.7µs    219.2µs
  UDS往返 envoy-in↔kitex-server       6.2µs   1.7%      4.3µs      8.4µs     10.1µs
  kitex-server 自身                  38.3µs  10.2%     36.2µs     44.2µs     61.8µs
  ───────────────                  ────────  ─────   ────────   ────────   ────────
  合计（仅 avg 可加）               373.7µs 100.0%          —          —          —
  端到端（client 总时长）           373.7µs           357.8µs    458.7µs    529.8µs

── 这两个量到底是什么

  设「N 总」= 节点 N 观测到的最早点到最晚点，
    「N 等」= 节点 N 花在等下一跳身上的时间，取值为：
        Envoy 各跳     up_writev_done → up_epoll_wake         （请求真正写出 → 响应把 epoll 唤醒）
        kitex-client   mesh_socket_read_start → mesh_np_epoll_wake
      **两端都收在「本机与内核交接」那一刻**：起点不取入队（那时请求还没离开本机），
      终点不取首字节可读（那已经在 readv、buffer、派发之后）。两头的本地开销
      算进「往返」的话，会同时高估链路、低估节点自身。

  **「N 自身」= N 总 − N 等**
      N 真正占用 CPU 的时间：解析、路由、编解码、业务逻辑。
      **不含**它坐等下一跳的那段。链路末端（server）不等任何人，自身 = 总。

  **「A↔B 往返」= A 等 − B 总**
      A 在等 B 的那段里，扣掉 B 自己干活的时间，剩下的就是数据在两者之间
      走了一个来回（去程 + 回程）。两项各自在本机用单调钟测得，
      相减时时钟偏斜完全抵消 —— 这是跨机也成立的原因（§8.2.3）。

  两式交替套用即可从 client 一路推到 server，各段相加恰好等于端到端
  （望远镜求和：中间每个「N 总」都被加一次减一次）。

  三条限制：
  1. **只有 avg 列能相加**。p50/p90/p99 描述的是**每一段自己的分布**，
     可以横向比较（哪一段尾巴长），但**不能纵向相加** —— Σp50(各段) ≠ p50(总)，
     所以那三列不给百分比，合计行也留空。想验证就对比最后两行。
  2. **「往返」不可拆分单向**：跨机是物理限制（需 PTP + 硬件时间戳网卡）；
     同机 UDS 同理 —— 差值法给出的本来就是一个来回。
  3. **「往返」不是纯线路时间**：B 起算点之前的排队（listener/worker 队列、
     goroutine 调度）落在 B 的测量区间之外，全被这个差值吸收。
     实测 Envoy worker 从 384 压到 2 时，同机 UDS「往返」从 21µs 涨到 128µs——
     多出来的一百微秒是排队不是传输。低负载无排队时才近似真实传输时间。

── 各节点观测区间（**嵌套，不可相加**，差值法的原始输入）
区间                          avg        p50        p90        p99        max
链路开销(往返)                336.3µs    319.4µs    419.7µs    488.9µs    3.014ms
envoy-in                118.9µs    110.3µs    126.5µs    266.4µs    638.8µs
envoy-out               347.7µs    333.4µs    407.7µs    498.8µs    858.1µs
kitex-client            374.6µs    357.8µs    459.2µs    530.7µs    3.047ms
kitex-server             38.3µs     36.2µs     44.2µs     61.8µs    576.0µs

注：涉及跨机，「链路开销」为往返总和，不可拆分单向（§8.2.4）
